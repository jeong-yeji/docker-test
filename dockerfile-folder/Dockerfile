# 베이스 이미지를 명시해준다.
# base image : 이미지 생성 시 기반이 되는 이미지 레이어
FROM alpine

# RUN : docker image 생성 전 수행할 쉘 명령어
# 추가적으로 필요한 파일들을 다운로드 받는다.
# RUN command

# CMD : 컨테이너 시작 시 실행할 실행 파일 또는 셸 스크립트
# 이 명령어는 도커파일 내에서 1회만 사용 가능
# 컨테이너 시작 시 실행 될 명령어를 명시해준다.
CMD ["echo", "hello"]